version: '3'
services:
  crowi:
    image: crowi
    tty: true
    environment:
      - DEBUG="crowi:*"
      - PASSWORD_SEED="password"
      - MONGO_URI="mongodb://172.21.39.74:27017/crowi"
      - FILEUPLOAD="local"
      - HTTP_PROXY="http://proxy.noc.kochi-tech.ac.jp:3128"
      - HTTPS_PROXY="http://proxy.noc.kochi-tech.ac.jp:3128"
      - http_proxy="http://proxy.noc.kochi-tech.ac.jp:3128"
      - https_proxy="http://proxy.noc.kochi-tech.ac.jp:3128"
    volumes:
      - ./:/crowi
      - /crowi/node_modules
    ports:
      - 3000:3000
    links:
      - mongodb
    command: bash -c "npm install && npm start"

  mongodb:
    image: mongo:3.6.3
    volumes:
      - ./data/mongodb:/data/db
    restart: always
    ports:
      - 27017:27017
